import RepositoryDetails from "../components/detailspage/RepositoryDetails";
import Layout from "../components/layout/Layout";

const RepositoryDetailsPage = (): JSX.Element => {
  return (
    <Layout>
      <RepositoryDetails />
    </Layout>
  );
}

export default RepositoryDetailsPage;
